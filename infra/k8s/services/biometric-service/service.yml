apiVersion: v1
kind: Service
metadata:
  name: biometric-service
  namespace: sports-prediction
spec:
  selector:
    app: biometric-service
  ports:
    - port: 8003
      targetPort: 8003
  type: ClusterIP
