apiVersion: v1
kind: Service
metadata:
  name: model-serving
  namespace: sports-prediction
spec:
  selector:
    app: model-serving
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP
