apiVersion: apps/v1
kind: Deployment
metadata:
  name: model-serving
  namespace: sports-prediction
spec:
  replicas: 3
  selector:
    matchLabels:
      app: model-serving
  template:
    metadata:
      labels:
        app: model-serving
    spec:
      containers:
        - name: model-serving
          image: ghcr.io/pierrebond/sports-model-serving:latest
          ports:
            - containerPort: 8004
          envFrom:
            - configMapRef:
                name: sports-config
          env:
            - name: API_KEY
              valueFrom:
                secretKeyRef:
                  name: sports-api-key
                  key: API_KEY
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 30
            periodSeconds: 15
