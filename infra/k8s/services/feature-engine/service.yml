apiVersion: v1
kind: Service
metadata:
  name: feature-engine
  namespace: sports-prediction
spec:
  selector:
    app: feature-engine
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
