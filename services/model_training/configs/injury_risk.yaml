model:
  name: injury_risk_xgboost
  version: 1.0.0

hyperparameters:
  n_estimators: 200
  max_depth: 5
  learning_rate: 0.05
  subsample: 0.8
  colsample_bytree: 0.8
  min_child_weight: 3
  reg_alpha: 0.1
  reg_lambda: 1.0

data:
  target_column: target
  positive_threshold: 0.5
